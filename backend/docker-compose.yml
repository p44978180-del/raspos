services:
  postgres:
    image: postgres:16-alpine
    container_name: timacad_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: timacad_schedule
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set a unique PostgreSQL password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d timacad_schedule"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: timacad_redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    container_name: timacad_backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      PORT: "8080"
      DATABASE_URL: ${DATABASE_URL:?Set PostgreSQL connection URL with the configured password}
      ADMIN_API_TOKEN: ${ADMIN_API_TOKEN:-}
      REDIS_URL: "redis://redis:6379/0"
      PYTHON_BIN: "python3"
      PARSER_SCRIPT_PATH: "/app/scripts/timacad_parser.py"
      SCHEDULE_DATA_PATH: "/app/public/data/official-schedule.json"
      AUTO_SEED: "false"
    volumes:
      - ../scripts:/app/scripts:ro
      - ../public/data:/app/public/data:ro
      - ../downloads:/app/downloads:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  postgres_data:
  redis_data:
